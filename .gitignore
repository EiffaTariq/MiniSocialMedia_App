# Dependencies
node_modules/

# Environment variables (VERY IMPORTANT)
.env

# VS Code settings (optional)
.vscode/

# Logs (not present now, but common in Node apps)
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS generated files (safe default, even if you don't see them now)
.DS_Store
Thumbs.db

# Build folders (if React/Vite creates these later)
dist/
build/